// 이 파일은 스크립트로 생성됨
// 위치 : Solution/Packet/jinja2/cpp

#include "PacketDispatcher.h"

{% for name in file_names %}
#include <{{name}}.pb.h>
{% endfor %}

using namespace packet;

void PacketDispatcher::Initialize()
{
	// 초기화 코드 자동으로 생성됨
{% for name in req_names %}
	RegisterPacket<{{name}}>(PacketID::{{name}});
{% endfor %}
}